# C/C++ tools
{% if base_os == "alpine" %}
RUN apk add --no-cache cmake g++ gdb
{% else %}
RUN apt-get update && apt-get install -y --no-install-recommends \
    cmake \
    g++ \
    gdb \
    && apt-get clean && rm -rf /var/lib/apt/lists/*
{% endif %}
